---
- hosts: all
 
  tasks:
  - name: Install apache packages 
    apt:
      name: apache2
      state: present
  - name: ensure httpd is running
    service:
      name: httpd 
      state: started
