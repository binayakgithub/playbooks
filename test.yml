---
- hosts: all
 
  tasks:
  - name: Install apache packages 
    apt-get:
      name: httpd
      state: present
  - name: ensure httpd is running
    service:
      name: httpd 
      state: started
